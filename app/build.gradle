plugins {
    id 'com.android.library'
}

android {
    namespace 'com.myunity'
    compileSdk 32

    defaultConfig {
        targetSdk 32
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}